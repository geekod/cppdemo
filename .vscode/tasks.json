{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake",
            "command": "cmake",
            "args": [
                "."
            ],
            "type": "shell",
            "problemMatcher": [
                "$gcc"
            ],
            "options": {
                "cwd": "${workspaceRoot}"
            }
        },
        {
            "label": "make",
            "command": "make",
            "args": [],
            "type": "shell",
            "problemMatcher": [
                "$gcc"
            ],
            "options": {
                "cwd": "${workspaceRoot}"
            }
        }
    ]
}